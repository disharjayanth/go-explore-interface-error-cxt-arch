// +build tools

package architecture

// Above comment(*+build tools*) need to be exactly like that and new line after that tells that
// this file no need to be compiled
import _ "github.com/golang/mock/mockgen"
